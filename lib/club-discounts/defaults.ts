/** Legacy demo provider id used by seeded example records. */
export const DEMO_PROVIDER_ID = "demo";

/** Seeded discount ids from earlier builds — stripped from real club storage. */
export const LEGACY_SEED_DISCOUNT_IDS = new Set([
  "disc-1",
  "disc-2",
  "disc-3",
  "disc-4",
]);

/** Seeded redemption ids from earlier builds — stripped from real club storage. */
export const LEGACY_SEED_REDEMPTION_IDS = new Set([
  "red-1",
  "red-2",
  "red-3",
]);
